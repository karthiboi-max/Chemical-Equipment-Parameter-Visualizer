name: chem_visualizer_mobile
description: Chemical Equipment Visualizer (mobile)
publish_to: "none"

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^5.2.5
  csv: ^5.0.0
  fl_chart: ^0.55.2
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
